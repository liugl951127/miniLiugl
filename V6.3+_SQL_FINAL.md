# V6.3+ SQL 完整解决方案 (FINAL)

## 📊 总览

| 维度 | 数据 |
|------|------|
| SQL 文件数 | 1 个 (`sql/minimax-mysql-final.sql`) |
| 总行数 | 2228 |
| 文件大小 | 129 KB |
| DDL 表数 | 89 |
| 测试账号 | 5 |
| 业务租户 | 3 |
| 种子数据 | 96 条 (17 类) |
| 触发器 | 4 |
| 存储过程 | 4 |
| 函数 | 4 |
| 事件 | 2 |
| 视图 | 2 |
| 业务索引 | 10 |
| 数据库用户 | 3 |
| schema_version | 3 行 |

## 🚀 一键部署 (3 步)

```bash
# 1. 部署
./scripts/db-reset.sh

# 2. 验证
./scripts/verify-sql.sh

# 3. 启动后端
cd backend && mvn spring-boot:run -pl minimax-gateway
```

## 🛠️ SQL 内容详解

### Stage 1: DDL (89 张表)

| 模块 | 表数 | 主要表 |
|------|------|--------|
| admin | 8 | sys_user, sys_role, sys_menu, sys_dict, sys_config |
| auth | 14 | auth_login_log, oauth_client, sso_provider |
| ai | 12 | ai_chat_session, ai_prompt, ai_tool, ai_intent_keyword |
| agent | 5 | agent_task, agent_step, agent_memory |
| analytics | 6 | analytics_event, analytics_metric |
| chat | 4 | chat_message, chat_room |
| model | 5 | model_config, model_call_log |
| monitor | 7 | alert_rule, alert_channel, alert_event |
| multimodal | 3 | image_asset, voice_clone |
| pipeline | 4 | pipeline_node, pipeline_edge |
| rag | 5 | rag_kb, rag_doc, rag_chunk |
| ws | 2 | ws_session, ws_message |
| common | 4 | audit_log, file_upload, notification, schedule_job |
| collab | 4 | collab_room, collab_doc, collab_op |
| kg | 5 | kg_entity, kg_relation, kg_query |

### Stage 2: 测试账号 (5 个)

| Username | Password (BCrypt) | 角色 | 租户 |
|----------|-------------------|------|------|
| admin | `{BCRYPT_PLACEHOLDER}` (admin123) | SUPER_ADMIN | default (id=1) |
| demo | `{BCRYPT_PLACEHOLDER}` (demo123) | ADMIN | default |
| test | `{BCRYPT_PLACEHOLDER}` (test123) | USER | default |
| guest | `{BCRYPT_PLACEHOLDER}` (guest123) | USER | enterprise |
| vip | `{BCRYPT_PLACEHOLDER}` (vip123) | USER | startup |

> **注意**: `{BCRYPT_PLACEHOLDER}` 是占位符, 实际部署需用 `BCryptPasswordEncoder` 生成
> 沙箱模式 `AdminDataInitializer` 会自动用 BCrypt 覆盖

### Stage 3: V6.3+ 业务种子 (96 条 / 17 类)

| 类 | 数量 | 示例 |
|----|------|------|
| sys_user | 5 | admin/demo/test/guest/vip |
| tenant | 3 | default/enterprise/startup |
| api_key | 5 | dev/test/prod/admin/vip |
| ai_prompt | 8 | 通用助手/SQL 生成/翻译/摘要/营销/客服/数据分析 |
| rag_kb | 3 | 产品文档/技术规范/客户支持 |
| ai_tool | 6 | 文本生成/图片生成/语音合成/语音识别/翻译/数据分析 |
| alert_rule | 5 | CPU 高/内存高/磁盘满/响应慢/异常多 |
| alert_channel | 3 | 钉钉/邮箱/企业微信 |
| ai_chat_session | 5 | 用户最近对话 |
| kg_entity | 6 | 人物/组织/概念/事件/地点/产品 |
| kg_relation | 5 | 任职/合作/属于/参与/位于 |
| workflow | 4 | 客服/审批/数据处理/通知 |
| model_provider | 5 | OpenAI/DeepSeek/通义千问/Claude/自研 |
| cluster_node | 3 | master/worker1/worker2 |
| plugin | 6 | 时间/天气/计算器/翻译/搜索/提醒 |
| data_source | 4 | MySQL/PG/MongoDB/ClickHouse |
| ai_training_task | 3 | intent-classifier-v1/rag-embeddings-v2/dialogue-bert |

### Stage 4: 数据库对象 (高级)

#### 触发器 (4)
- `trg_sys_user_updated_at` - sys_user 更新自动设 updated_at
- `trg_api_key_prefix` - api_key 创建自动生成 key_prefix
- `trg_chat_session_cascade` - 删除 session 级联删 message
- `trg_alert_rule_notify` - 告警规则触发自动写 alert_event

#### 存储过程 (4)
- `sp_user_key_count(p_user_id, OUT o_total, OUT o_active)` - 用户 API Key 统计
- `sp_cleanup_audit(IN p_days)` - 清理 N 天前 audit_log
- `sp_monthly_report(IN p_year, IN p_month)` - 月度报告聚合
- `sp_rotate_api_key(IN p_old, IN p_user, OUT p_new)` - Key 轮换

#### 函数 (4)
- `fn_user_activity_score(p_user_id)` - 用户活跃度 (0-100)
- `fn_mask_phone(p_phone)` - 手机号脱敏 `138****0000`
- `fn_mask_email(p_email)` - 邮箱脱敏 `zh***@example.com`
- `fn_distance_km(lat1, lon1, lat2, lon2)` - Haversine 距离 (km)

#### 事件 (2)
- `evt_daily_cleanup_audit` - 每天 2 点清理 90 天前 audit
- `evt_refresh_dashboard` - 每 10 分钟刷新缓存

#### 视图 (2)
- `v_user_api_key_count` - 用户 API Key 数量统计
- `v_alert_active` - 在线告警列表

#### 业务索引 (10)
- `idx_sys_user_username` / `idx_sys_user_status`
- `idx_ai_chat_session_user` / `idx_api_key_user` / `idx_api_key_status`
- `idx_ai_prompt_category` / `idx_kg_entity_type`
- `idx_alert_rule_status` / `idx_workflow_status` / `idx_ai_tool_category`

#### 用户/权限 (3)
- `root@%` - 超级管理员
- `minimax_app@%` - 应用主账号 (CRUD)
- `minimax_ro@%` - BI 只读账号 (SELECT)

## 🔧 部署指南

### 方法 1: 一键脚本 (推荐)

```bash
./scripts/db-reset.sh
```

### 方法 2: 手动 (3 步)

```bash
# 1. 跑 SQL
mysql -uroot -pminimax_mariadb_2024 minimax_platform < sql/minimax-mysql-final.sql

# 2. 验证
bash scripts/verify-sql.sh

# 3. (可选) 加 minimax_app 应用用户
mysql -uroot -pminimax_mariadb_2024 -e "
GRANT SELECT, INSERT, UPDATE ON minimax_platform.* TO 'minimax_app'@'%';
FLUSH PRIVILEGES;"
```

### 方法 3: Docker 自动

```yaml
# docker-compose.yml
volumes:
  - ./sql/minimax-mysql-final.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
```

MariaDB 启动时自动执行, 0 人工介入。

## ✅ 验证清单

```sql
-- 1. 表数 (89+)
SELECT COUNT(*) FROM information_schema.tables 
WHERE table_schema = 'minimax_platform';
-- 期望: 89+

-- 2. 触发器 (4)
SHOW TRIGGERS FROM minimax_platform;
-- 期望: 4

-- 3. 存储过程 (4)
SHOW PROCEDURE STATUS WHERE Db = 'minimax_platform';
-- 期望: 4

-- 4. 函数 (4)
SHOW FUNCTION STATUS WHERE Db = 'minimax_platform';
-- 期望: 4

-- 5. 5 账号
SELECT id, username, nickname FROM sys_user 
WHERE username IN ('admin','demo','test','guest','vip') ORDER BY id;
-- 期望: 5 行

-- 6. 3 租户
SELECT id, code, name FROM tenant WHERE deleted = 0 ORDER BY id;
-- 期望: 3 行

-- 7. schema_version
SELECT * FROM schema_version ORDER BY id;
-- 期望: V6.3.0, V6.3.1, V6.3.2

-- 8. 测试脱敏函数
SELECT fn_mask_phone('13800001234');
-- 期望: 138****1234

-- 9. 测试活跃度
SELECT fn_user_activity_score(1);
-- 期望: 数字 (0-100)
```

## 🔄 升级路径

| 版本 | 改动 | SQL 段 |
|------|------|--------|
| V3.5.58 | 89 张表 + 5 账号 | Stage 1+2 |
| V6.3.0 | 10 字段 + 96 种子 + 10 索引 + 2 视图 | Stage 3 |
| V6.3.1 | 4 触发器 + 4 过程 + 4 函数 + 2 事件 | Stage 4 |
| V6.3.2 | 3 用户/权限 + schema_version | Stage 4 |

每次升级只需重跑 `minimax-mysql-final.sql`:
- `INSERT IGNORE` 保证种子幂等
- `DROP TABLE IF EXISTS` + `CREATE TABLE IF NOT EXISTS` 保证 DDL 幂等
- `CREATE OR REPLACE` 视图保证视图幂等
- `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER` 保证触发器幂等

## 🚨 故障排查

### Q1: 1062 - Duplicate entry
**A**: 已用 `INSERT IGNORE` 保护, 重复插入静默跳过

### Q2: 1091 - Can't DROP ... check that column/key exists
**A**: 用 `DROP TABLE IF EXISTS` 而非 `DROP TABLE`

### Q3: 1227 - Access denied
**A**: `minimax_app` 账号需要 `GRANT EXECUTE ON PROCEDURE`

### Q4: 函数不存在
**A**: MySQL 5.7+ 才支持 `CREATE FUNCTION`, 5.6 需 `SET GLOBAL log_bin_trust_function_creators=1`

## 📜 演进历史

| Commit | 说明 |
|--------|------|
| `1b46db0` | v6.3-enhancements.sql (字段 + 种子 + 索引 + 视图) |
| `5b97cd1` | 18 大类种子 (254 行) |
| `2703b30` | v6.3-triggers.sql (210 行) |
| `4e637ff` | SQL 集中到 sql/ 目录 |
| `e254be4` | 合并为 1 个 minimax-mysql-final.sql |
| `1c807b1` | 修 Duplicate entry (INSERT IGNORE) |
| `f623d8c` | verify-sql.sh (10 步验证) + db-reset 简化 |
| `a06ef97` | GitHub Actions verify-sql workflow |
