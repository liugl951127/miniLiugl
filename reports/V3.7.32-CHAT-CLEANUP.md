# V3.7.32+ MessageController 清理 + 全栈 sendBusiness 统一

## 1. MessageController 清理 unused

**V3.7.31+ 改 sendBusiness 后残留**：
- ❌ `Long userId = principal != null ? principal.id() : null;` 未使用
- ❌ `import java.util.LinkedHashMap;` 未使用

V3.7.32 清理：
```java
// 之前
Long userId = principal != null ? principal.id() : null;
SseEmitter emitter = new SseEmitter(120_000L);

// V3.7.32+
SseEmitter emitter = new SseEmitter(120_000L);
```

## 2. 全栈 SseResult 调用统一

| 范围 | sendBusiness | sendDone | sendError | SseResult.send (老) |
|------|-------------|----------|-----------|---------------------|
| chat MessageController | ✅ start/content | ✅ done | ✅ error | ❌ 0 |
| agent AgentService | ✅ thought/tool-call/observation/final 等 | ✅ done | ✅ error | ❌ 0 |
| ai StreamingMusicGen | ✅ heartbeat/start/chunk/progress/complete | - | ✅ error | ❌ 0 |
| ai StreamingVideoGen | ✅ heartbeat | - | ✅ error | ❌ 0 |

**全栈 0 处用 `SseResult.send`（老 API）**。

## 3. 4 个 API 各司其职

```java
// 业务 event 入口 (自动查 type)
SseResult.sendBusiness(emitter, "content", Map.of("content", "..."));

// 标准 5 type 显式 (路由表里没有的)
SseResult.sendCustom(emitter, "my-event", "content", data);

// 专用: done
SseResult.sendDone(emitter);

// 专用: error
SseResult.sendError(emitter, "msg");

// 完成 emitter
SseResult.complete(emitter);
```

## 累计 117 个版本 (V3.5.46-V3.7.32)
