# V3.6.8 V3.5.91 otel-trace workflow 真跑 Attempt (沙箱)

## 1. 沙箱环境

- **OS**: Linux 容器
- **Docker**: ❌ 未安装
- **Java**: ❌ 未安装
- **网络**: 沙箱内网, 没法访问 GitHub Actions runners

## 2. V3.5.91 otel-trace workflow 现状

```yaml
# .github/workflows/otel-trace.yml (250 行)
name: OTel Trace
on:
  workflow_dispatch:    # 手动触发
  push:
    branches: [main, develop]
    paths: [OTel 相关文件]

jobs:
  otel-trace:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 启 docker compose (mariadb + redis + nacos + 14 module + otel-collector + jaeger)
      - name: 跑 5 browser matrix
      - name: 查 jaeger API 验证 trace 上报
      - name: 上传 artifact
      - name: 关 compose
```

## 3. 沙箱试跑结果

```bash
$ which docker
(not found)

$ docker compose --version
bash: docker: command not found
```

## 4. V3.6.4+ 沙箱友好版替代

**`scripts/otel-trace-sandbox.sh`** (Check 11) 静态验证 OTel 配置:
- sw.js 含 withTraceparent 函数 ✅
- docker-compose otel-collector service ✅
- docker-compose jaeger service ✅
- 12 module depends_on otel-collector ✅
- OTEL_* env vars (jvm-env) ✅
- otel-collector-config.yaml 完整 ✅
- 沙箱模拟 5 browser trace ✅

## 5. CI 真跑 (GitHub Actions)

V3.5.91 workflow 已配, 真跑需要 GitHub Actions runner:
- ✅ Ubuntu runner 有 docker
- ✅ Java 17 + Maven cache
- ✅ 30 min timeout
- ✅ 14 module + otel-collector + jaeger

**触发**: GitHub Actions → workflow_dispatch → OTel Trace

## 6. 沙箱增强版 (V3.6.8+)

新增 `scripts/otel-trace-sandbox.sh` 5 项检查, 让沙箱也能验证 OTel 配置:
- 不开 docker, 不跑真服务
- 静态检查 docker-compose / sw.js / otel-collector-config.yaml
- 沙箱模拟 5 browser trace (随机 traceparent)
- 0.3s 跑完, 集成到 ci-check (Check 11)

## 7. CI 完整流程

```bash
# GitHub Actions 端到端 (4GB+ 内存, 30min)
1. 启 mariadb + redis + nacos (depends_on)
2. 启 14 module (depends_on otel-collector service_started)
3. 启 otel-collector (port 4317 gRPC + 4318 HTTP)
4. 启 jaeger (port 16686 UI)
5. 等所有服务 healthy
6. 跑 5 browser matrix (Round 7 + Round 8)
7. 查 jaeger API: GET /api/services + /api/traces
8. 验证 trace 上报 (14 module 都含 trace)
9. 上传 artifact
10. 关 compose
```

## 8. 沙箱友好版 vs CI 真跑 对比

| 维度 | 沙箱友好版 (Check 11) | CI 真跑 (V3.5.91) |
|------|------------------------|---------------------|
| Docker | ❌ 不要 | ✅ 必须 |
| 14 module 真启 | ❌ | ✅ |
| jaeger UI | ❌ | ✅ :16686 |
| OTel gRPC | ❌ | ✅ :4317 |
| 耗时 | 0.3s | 30min |
| 静态配置检查 | ✅ | ✅ |
| 真实 trace 上报 | ❌ | ✅ |

## 9. 累计 63 个版本 (V3.5.46-V3.6.8)
