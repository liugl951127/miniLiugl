# V3.6.25 后端 14 module mvn install 沙箱 docker 入口 attempt

## 1. 沙箱环境

| 工具 | 状态 |
|------|------|
| Java (openjdk-17) | ❌ 未装 |
| Maven (3.9) | ❌ 未装 |
| Docker (maven 镜像) | ❌ 未装 |
| 内存 | 2 GB |

## 2. 尝试

```bash
$ bash scripts/mvn-docker.sh --version
❌ 沙箱无 docker
```

## 3. 替代方案

### 3.1 CI 端到端 (V3.5.65+ backend job)

```yaml
# .github/workflows/backend-build.yml
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - run: cd backend && mvn -B -T 4 clean install
      - uses: actions/upload-artifact@v4
        with:
          name: target
          path: '**/target/*.jar'
```

### 3.2 本地开发

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y openjdk-17-jdk maven

# macOS
brew install openjdk@17 maven

# 编译
cd backend
mvn -B -T 4 clean install
```

### 3.3 静态检查 (沙箱友好)

```bash
# Check 10: pom.xml 一致性
bash scripts/check_pom_consistency.py

# 14 module artifactId 跟目录名一致性
# Spring Boot 3.x + Java 17 + Spring Cloud 2023.x
```

## 4. 14 module 列表 (V3.5.18+ 合并后)

| # | 模块 | 端口 |
|---|------|------|
| 1 | minimax-gateway | 8080 |
| 2 | minimax-auth | 9001 |
| 3 | minimax-chat | 9005 |
| 4 | minimax-model | 9003 |
| 5 | minimax-agent | 9010 |
| 6 | minimax-rag | 9006 |
| 7 | minimax-multimodal | 9088 |
| 8 | minimax-pipeline | 9085 |
| 9 | minimax-monitor | 9089 |
| 10 | minimax-admin | 9087 |
| 11 | minimax-analytics | 9090 |
| 12 | minimax-audit | 9091 |
| 13 | minimax-ai | (合并) |
| 14 | minimax-common | (共享) |

## 5. 累计 80 个版本 (V3.5.46-V3.6.25)
