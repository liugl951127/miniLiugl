# V3.7.33+ SseResult 单元测试 + SseUtil 别名类 + 注释清理

## 1. 单元测试 SseResult 6 个方法（V3.7.33+）

`backend/minimax-common/src/test/java/com/minimax/common/sse/SseResultTest.java`：

| # | 测试 | 验证 |
|---|------|------|
| 1 | test1_sendStandard5Type | send 标准 5 type |
| 2 | test2_sendBusiness_autoRouteType | sendBusiness 自动查 type (content/tool_call/source) |
| 3 | test3_sendCustom_explicitType | sendCustom 显式 event+type |
| 4 | test4_sendError | sendError |
| 5 | test5_sendDone | sendDone |
| 6 | test6_complete_andCompleteWithError | complete / completeWithError |
| 7 | test7_eventToType_routing | EVENT_TO_TYPE 16 个路由 + 兼容别名 |
| 8 | test8_sendBusiness_compatibleAlias | 4 个老别名 (chunk/toolcall/src/err) |
| 9 | test9_sendError_codeIs1 | sendError 不抛异常 |
| 10 | test10_sendDone_codeIs0 | sendDone 不抛异常 |

## 2. 注释清理（V3.7.33+）

SseResult.java 头部 javadoc 改：
```java
// V3.7.32-
SseResult.send(emitter, "content", ...)  // 5 type 标准

// V3.7.33+
SseResult.sendBusiness(emitter, "thought", ...)  // 业务自动路由
SseResult.sendBusiness(emitter, "content", ...)  // 标准 5 type
SseResult.sendDone(emitter)
SseResult.sendError(emitter, "stream failed")
SseResult.complete(emitter)
```

## 3. SseUtil 别名类（V3.7.33+）

`backend/minimax-common/src/main/java/com/minimax/common/sse/SseUtil.java`：
- 语义更清楚（工具类）
- 委托 SseResult
- 6 个方法别名
- 老代码继续用 SseResult（向后兼容）

```java
// 推荐新代码
SseUtil.sendBusiness(emitter, "thought", data);
SseUtil.sendDone(emitter);
SseUtil.sendError(emitter, "err");
SseUtil.complete(emitter);

// 老代码继续用
SseResult.sendBusiness(emitter, "thought", data);  // 完全一样
```

## 累计 118 个版本 (V3.5.46-V3.7.33)
