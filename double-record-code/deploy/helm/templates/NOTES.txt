🎉 双录一体化平台部署成功！

验证步骤:
  kubectl get pods -l app.kubernetes.io/name={{ include "dual-record.name" . }}
  kubectl get svc {{ include "dual-record.fullname" . }}
  kubectl get ingress {{ include "dual-record.fullname" . }}

健康检查:
  curl -k https://{{ (index .Values.ingress.hosts 0).host }}/actuator/health

查看日志:
  kubectl logs -f -l app.kubernetes.io/name={{ include "dual-record.name" . }}

清理:
  helm uninstall {{ .Release.Name }}

更多信息:
  📚 https://github.com/liugl951127/recordInfo
  📧 mavis@bank.com
