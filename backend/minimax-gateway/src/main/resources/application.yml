# =============================================================
# gateway 模块配置 (V5.4 重构后)
# 公共配置在 application-common.yml
# =============================================================

server:
  port: 8080
  servlet:
    context-path: /

spring:
  config:
    import: classpath:application-common.yml
  application:
    name: minimax-gateway
  profiles:
    active: dev

# gateway 特有配置 (跨平台路由 + 文件上传 + minio)
minimax:
  jwt:
    expire-minutes: 10080
    refresh-expire-minutes: 43200
    issuer: minimax-platform
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://localhost:5174
      - http://127.0.0.1:5173
  upload:
    base-dir: ${UPLOAD_DIR:./data/upload}
    max-size-mb: 100
  minio:
    enabled: false
    endpoint: http://127.0.0.1:9000
    access-key: minimax_admin
    secret-key: minimax_minio_2024
    bucket: minimax-files