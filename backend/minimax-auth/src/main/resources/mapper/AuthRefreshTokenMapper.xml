<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minimax.auth.mapper.AuthRefreshTokenMapper">

    <update id="revokeByToken">
        UPDATE auth_refresh_token SET revoked = 1 WHERE token = #{token}
    </update>

    <update id="revokeByUserId">
        UPDATE auth_refresh_token SET revoked = 1 WHERE user_id = #{userId}
    </update>
</mapper>
