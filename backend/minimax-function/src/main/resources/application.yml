# =============================================================
# function 模块配置 (V5.4 重构后)
# 公共配置在 application-common.yml, 这里只放模块特有配置
# =============================================================

server:
  port: 8086
  servlet:
    context-path: /api/v1

spring:
  config:
    import: classpath:application-common.yml
  application:
    name: minimax-function
  profiles:
    active: dev

# 模块特殊配置 (保留原 minimax.* 块)
minimax:
  # (无特殊配置, 模块按需添加)
  # (无特殊配置)
